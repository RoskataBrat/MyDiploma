{
  "version": 2,
  "buildCommand": "mkdir -p build && echo placeholder > build/index.html",
  "outputDirectory": "build",
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "server.js"
    }
  ]
}